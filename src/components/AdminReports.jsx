import {useEffect,useState} from 'react'
import {supabase} from '../lib/supabase'
const cards=[['Customers','customers'],['Technicians','technicians'],['Sites','sites'],['Devices','devices'],['AMC Contracts','amc'],['Complaints','complaints'],['Service History','service_history'],['Products','products'],['Serials','product_serials'],['Stock Movements','stock_movements']]
const groups={workload:['Complaints','Service History'],infrastructure:['Customers','Sites','Devices'],inventory:['Products','Serials','Stock Movements'],amc:['AMC Contracts']}
export default function AdminReports({onBack}){const [counts,setCounts]=useState({}),[loading,setLoading]=useState(true),[error,setError]=useState(''),[selected,setSelected]=useState(null);useEffect(()=>{let live=true;(async()=>{const out=await Promise.all(cards.map(async([label,table])=>{const r=await supabase.from(table).select('id',{count:'exact',head:true});return[label,r.error?null:r.count??0]}));if(live){setCounts(Object.fromEntries(out));if(out.some(([,v])=>v===null))setError('Some report counts could not be loaded. Check table permissions.');setLoading(false)}})();return()=>{live=false}},[]);const total=(keys)=>keys.reduce((n,k)=>n+(Number(counts[k])||0),0);const reportItems=(key)=>groups[key].map(label=>({label,value:loading?'—':counts[label]??0}));const reportMeta={workload:['Service Workload','Service requests and completed service history'],infrastructure:['Customer Infrastructure','Customers, sites and installed devices'],inventory:['Inventory & Serials','Products, serial numbers and stock movement'],amc:['AMC Portfolio','Active and managed AMC contracts']};return <section className='admin-panel'><div className='panel-heading'><div><span className='badge'>ADMIN • REPORTS</span><h2>Reports & Analytics</h2><p>Live operational summary from Supabase.</p></div><button className='secondary' onClick={onBack}>← Dashboard</button></div>{error&&<p className='error'>{error}</p>}<div className='admin-stats'>{cards.map(([label])=><div className='stat-card' key={label}><strong>{loading?'—':counts[label]??'—'}</strong><span>{label}</span></div>)}</div><div className='admin-form'><h3>Key Operational Totals</h3><p className='muted'>Click any report to open its detailed breakdown.</p><div className='data-list'>{Object.entries(reportMeta).map(([key,[title,desc]])=><article key={key} onClick={()=>setSelected(key)} role='button' tabIndex={0} onKeyDown={e=>e.key==='Enter'&&setSelected(key)} style={{cursor:'pointer'}}><strong>{title}</strong><span>{loading?'—':total(groups[key])} {key==='amc'?'contracts':'records'}<small>{desc}</small></span></article>)}</div>{selected&&<div className='admin-form' style={{marginTop:16}}><div className='panel-heading'><div><h3>{reportMeta[selected][0]}</h3><p className='muted'>{reportMeta[selected][1]}</p></div><button className='secondary' onClick={()=>setSelected(null)}>Close</button></div><div className='data-list'>{reportItems(selected).map(item=><article key={item.label}><strong>{item.label}</strong><span>{item.value} records</span></article>)}</div></div>}</div></section>}