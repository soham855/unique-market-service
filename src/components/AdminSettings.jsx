import {useEffect,useState} from 'react'
import {supabase} from '../lib/supabase'
export default function AdminSettings({onBack}){
 const [saved,setSaved]=useState(false),[settings,setSettings]=useState({maintenance:false,topology:false,notifications:true,upi_id:'',account_name:'',qr_image_url:'',is_enabled:true})
 useEffect(()=>{(async()=>{const [{data:s}]=await Promise.all([supabase.from('payment_settings').select('upi_id,account_name,qr_image_url,is_enabled').limit(1).maybeSingle()]);if(s)setSettings(x=>({...x,...s}))})()},[])
 function update(k,v){setSettings(s=>({...s,[k]:v}));setSaved(false)}
 async function save(){Object.entries(settings).filter(([k])=>['maintenance','topology','notifications'].includes(k)).forEach(([k,v])=>localStorage.setItem(`um_${k}`,String(v)));const {data:user}=await supabase.auth.getUser();const {data,error}=await supabase.from('payment_settings').select('id').limit(1).maybeSingle();if(error){setSaved(false);return}const payload={upi_id:settings.upi_id.trim()||null,account_name:settings.account_name.trim()||null,qr_image_url:settings.qr_image_url.trim()||null,is_enabled:settings.is_enabled,updated_by:user.user?.id||null,updated_at:new Date().toISOString()};const result=data?await supabase.from('payment_settings').update(payload).eq('id',data.id):await supabase.from('payment_settings').insert(payload);if(!result.error)setSaved(true)}
 return <section className='admin-panel'><div className='panel-heading'><div><span className='badge'>ADMIN • SETTINGS</span><h2>System Settings</h2><p>Control operational features and the Unique Market payment account.</p></div><button className='secondary' onClick={onBack}>← Dashboard</button></div><div className='admin-form'><label>Maintenance mode <input type='checkbox' checked={settings.maintenance} onChange={e=>update('maintenance',e.target.checked)}/></label><label>Visual Network Topology <input type='checkbox' checked={settings.topology} onChange={e=>update('topology',e.target.checked)}/></label><label>Admin notifications <input type='checkbox' checked={settings.notifications} onChange={e=>update('notifications',e.target.checked)}/></label></div><hr/><div><span className='badge'>PAYMENT • UPI / QR</span><h3>Customer UPI Account</h3><div className='admin-form'><label>UPI ID<input value={settings.upi_id||''} onChange={e=>update('upi_id',e.target.value)} placeholder='yourname@upi'/></label><label>Account / Business Name<input value={settings.account_name||''} onChange={e=>update('account_name',e.target.value)} placeholder='Unique Market'/></label><label>QR Image URL <input value={settings.qr_image_url||''} onChange={e=>update('qr_image_url',e.target.value)} placeholder='Optional hosted QR image URL'/></label><label>Enable customer UPI <input type='checkbox' checked={settings.is_enabled!==false} onChange={e=>update('is_enabled',e.target.checked)}/></label><div><button onClick={save}>Save Settings</button>{saved&&<span className='muted'> Saved</span>}</div></div></div></section>
}